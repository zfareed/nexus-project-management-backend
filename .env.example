# Database Configuration
# Prisma Database URL (PostgreSQL)
DATABASE_URL="postgresql://postgres:your_password_here@localhost:5432/nexus-app?schema=public"

# Legacy Database Configuration (for reference)
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=your_password_here
DB_DATABASE=nexus-app

# Application
PORT=3000
NODE_ENV=development

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_EXPIRES_IN=7d
